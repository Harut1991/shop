# Production Environment Variables for Shop Admin Panel
# Copy this file to .env.production and update with your values

# Server Port (Render uses PORT env var automatically, usually 10000)
PORT=10000

# JWT Secret Key for token signing - CHANGE THIS IN PRODUCTION!
# Generate a secure key with: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your-secret-key-change-in-production

# Node Environment
NODE_ENV=production

# Database Path (optional, defaults to ./shop.db)
# DATABASE_PATH=./shop.db

